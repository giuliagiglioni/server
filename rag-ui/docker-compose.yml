services:
  rag-ui:
    build: .
    container_name: rag-ui
    restart: unless-stopped
    environment:
      - RAG_API_BASE_URL=https://141.250.40.120/rag/api
      - RAG_API_TIMEOUT=60
    networks:
      - llm-net

networks:
  llm-net:
    external: true
